---
title: "R Notebook"
output: html_notebook
---

This is an [R Markdown](http://rmarkdown.rstudio.com) Notebook. When you execute code within the notebook, the results appear beneath the code. 

Try executing this chunk by clicking the *Run* button within the chunk or by placing your cursor inside it and pressing *Cmd+Shift+Enter*. 

```{r}
data <- read_tsv('src/pbmc_cell_expression.tsv.gz')
```

Part1:
- Create an app that allows you to pick either the cell_type column, or the patient_id column, and makes violin plots of each gene's expression for value in the column, using facet 
Things you'll need:

- re-shape the data for plotting, 
- write out code for plotting, and 
- add elements to the shiny app for:
    - selecting the columns and 
    - drawing the plots 

Part2:
Make a 3d- scatter plot using, using the output of PCA with plotly
- run the pca, make the plot
- use `plotlyOutput` in the UI
- use `renderPlotly` in the server 
-




